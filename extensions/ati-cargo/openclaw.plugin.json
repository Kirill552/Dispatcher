{
  "id": "ati-cargo",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiToken": {
        "type": "string",
        "description": "ATI.su API Bearer token"
      },
      "boardId": {
        "type": "string",
        "description": "ATI board ID for cargo publishing (optional, auto-fetched from API if not set)"
      },
      "monitorIntervalMs": {
        "type": "number",
        "description": "Polling interval for response monitoring in ms",
        "default": 30000
      },
      "mockMode": {
        "type": "boolean",
        "description": "Тестовый режим: не публиковать реальные грузы в ATI, возвращать локальные mock-ответы",
        "default": false
      }
    },
    "required": ["apiToken"]
  }
}
